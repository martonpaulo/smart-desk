# Smart Desk Supabase configuration
# Use relative directories to align with monorepo layout

# Set your project ref here when available
project_id = ""

[db]
schemas = ["public"]

[migrations]
# Directory for SQL migrations relative to this file
directory = "./migrations"

# Optional: per-function settings go here (one table per function)
# [functions.purge-soft-deleted]
# verify_jwt = false
# import_map = './functions/purge-soft-deleted/import_map.json'

[edge_runtime]
# configure edge runtime policy if needed
# policy = "oneshot"
