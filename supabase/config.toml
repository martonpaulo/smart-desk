# supabase/config.toml — minimal, valid for CLI v2+
project_id = "dckaobffdeqsyzzdiwpq"

[db.migrations]
enabled = true
# relative to the supabase/ directory
schema_paths = ["./migrations"]

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]

# function-specific config must be under functions.<name>
# [functions.my-fn]
# verify_jwt = false
